<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Blog Website </title>
</head>

<body>
    <h1>This is heading </h1>


    <div>
        <ul>
            <% if(locals.blogs) { %>
                        <% blogs.forEach((blog)=>{ %>

                 <li>
      <a href="/blog/<%= blog.id %>">
        <%= blog.title %>
      </a>

                  

                        <% }) %> 
                        
                        <% } else{%>
                          <h1>  write something </h1>



                        <% } %>
         
        </ul>

    </div>


    <!-- <a href="/submit"> New Blog </a>  -->
   <button onclick="window.location.href='/newBlog'">
Create New Blog
</button>
</body>

</html>